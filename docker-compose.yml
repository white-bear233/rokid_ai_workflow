version: "3.8"

services:
  ai-workflow:
    image: ai-workflow:latest
    container_name: ai-workflow
    restart: always

    ports:
      - "8010:8000"

    environment:
      HOST: 0.0.0.0
      PORT: 8000
      WORKERS: 4

    # 从当前目录的 .env 文件加载环境变量
    env_file:
      - .env